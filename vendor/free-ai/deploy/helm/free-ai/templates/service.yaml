apiVersion: v1
kind: Service
metadata:
  name: {{ include "free-ai.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "free-ai.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "free-ai.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
