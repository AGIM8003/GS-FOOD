# Strategic merge: inject env from Kubernetes Secret (create out-of-band; never commit values).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: free-ai
spec:
  template:
    spec:
      containers:
        - name: engine
          envFrom:
            - secretRef:
                name: free-ai-engine-secrets
